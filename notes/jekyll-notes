sudo gem install rails
sudo gem install jekyll
sudo gem install jekyll-display-medium-posts
sudo gem install jekyll bundler
cd ~/desiredFolder
jekyll new <foldername>
cd <foldername> OR 
bundle init
bundle install
bundle add jekyll
bundle add webrick
bundle exec jekyll serve